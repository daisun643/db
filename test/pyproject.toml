[project]
name = "test"
version = "0.1.0"
description = "Tongji Forum API integration tests"
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.1.0",
    "requests>=2.34.2",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
